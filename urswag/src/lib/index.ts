// place files you want to import through the `$lib` alias in this folder.
export { default as Clock } from "$components/Clock.svelte";
export { default as HomeSection } from "$components/sections/HomeSection.svelte";